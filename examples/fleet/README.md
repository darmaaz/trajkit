# Fleet example

The original fleet pipeline that motivated this library will be ported
here as a case study once the library reaches v0.1.0. It will demonstrate
the full discretize → embed → compare loop on a real logistics fleet.

Until then, see the upstream `fleet-intelligence` repository.
