# Geolife example

Microsoft Geolife is the v1 cross-domain validation dataset (see
`docs/design/LIBRARY.md` §17). A notebook + integration test land
alongside v0.1.0 to demonstrate that the same `clean → segment →
episode → embed → compare` pipeline that works on a logistics fleet
also produces sane results on pedestrian traces.
