"""Per-domain default ``Params`` bundles.

See ``docs/design/LIBRARY.md`` §9 (Domain extensibility). Bundles for
``logistics_vehicle`` and ``pedestrian`` land in v0.1.0 alongside the
modules whose Params they bundle.
"""

SCALE_PRESETS: dict[str, object] = {}
