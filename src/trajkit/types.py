"""Canonical Pandera + Arrow schemas for all public DataFrames.

Single source of truth for the data contract. See
``docs/design/schemas.md`` for the full reference.

The schemas in this module are placeholders for v0.0.1 — the full
declarations land alongside the first L1 module implementation
(``trajkit.clean``).
"""

# Schemas declared per-module as implementation progresses.
